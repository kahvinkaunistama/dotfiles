-- lazy.nvim
return {
    "folke/zen-mode.nvim",
    opts = {
        window = {
            width = 80, -- how many characters wide the editing area is
            options = {
                number = true,
                relativenumber = true,
            },
        },
    }
}
